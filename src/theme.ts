import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#56cfe1", 
    },
    secondary: {
      main: "#f50057", 
    },
    background: {
      default: "#202024", 
      paper: "#323238", 
    },
    text: {
        primary: "#ffffff",
        secondary: "#b0b0b0",
      },
  },
});
